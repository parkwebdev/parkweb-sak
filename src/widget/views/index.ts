/**
 * Widget View Components
 * 
 * View components representing distinct screens/states of the widget.
 * Each view handles its own layout and content rendering.
 * 
 * @module widget/views
 */

/** Home screen with announcements and quick actions */
export { HomeView } from './HomeView';

/** Conversation list for returning users */
export { MessagesView } from './MessagesView';

/** Active chat conversation view */
export { ChatView } from './ChatView';

/** Help center with categories and articles */
export { HelpView } from './HelpView';
